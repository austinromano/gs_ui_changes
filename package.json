{
  "name": "ghost-session",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @ghost/types run build && pnpm --filter @ghost/protocol run build",
    "start": "pnpm run build && cd apps/server && npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=20"
  }
}
